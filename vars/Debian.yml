# roles/memcached/vars/Debian.yml
#
# Distribution-specific variables for Debian, Ubuntu, ...
---
memcached_user: memcache

memcached_packages:
  - memcached

memcached_service: memcached

memcached_config_file: /etc/memcached.conf
