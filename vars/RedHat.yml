# roles/memcached/vars/RedHat.yml
#
# Distribution-specific variables for RHEL, CentOS, ...
---
memcached_user: memcached

memcached_packages:
  - memcached

memcached_service: memcached

memcached_config_file: /etc/sysconfig/memcached
